/**
 * Naming Conventions
 * 
 * 변수 이름 지을때
 * 1) 일반적으로 영어(라틴문자)를 사용하며 문자와 숫자를 모두 사용할 수 있다.
 * 2) 특수기호는 언더바와 달러만 사용가능
 * 3) 숫자로 이름을 시작할 수 없다,
 *    ex) 1Name,2Hello
 * 4) 키워드는 변수명으로 사용할 수 없다.
 *    ex) var var = '';
 */

let codefactory = '코드팩토리';
var $ive = '아이브';
const _yuJin = '안유진';

console.log(codefactory);
console.log($ive);
console.log(_yuJin);

// let 1name= 'no';
// let const = 'attention';

/**
 * Naming Convetion2
 * 
 * 1) camelCase - 첫번째 글자는 소문자, 다음 띄어쓰기부터 대문자로,  대부분의 언어에서 많이 사용
 * 2) snake_case - 소문자만 사용, 띄어쓰기는 언더바로, 파이썬 같은 언어에서 사용
 * 3) PascalCase - 첫번쨰 글자도 대문자, C#, MicroSoft 계열의 언어에서 사용함
 */

const anYuJin = '안유진';